# My Personal Hub

## Stack
- Frontend: Next.js 16, React 19, TypeScript, Tailwind 4, shadcn, Recharts
- Backend: FastAPI, SQLAlchemy (async), PostgreSQL, Alembic
- Deploy: Vercel (frontend), Railway (backend)
- AI: OpenAI, Anthropic, Google Generative AI
- Integrations: Telegram (Telethon + Bot API), Garmin Connect, Google Calendar, Google Drive, Gmail

## Current Status

- **Mode:** Redesign **Stage 5 — Plan mode → ready for Build** (2026-04-20). PRD committed (`2a1834e`) + pre-build audit patch (`b37bce3`) on `main`. Plan at `docs/plans/2026-04-20-redesign-stage-5-mobile-qa.md` (gitignored, 22 tasks). Scope: mobile adaptive (≤960px per `handoff/HANDOFF.md §8`) + PWA iOS polish (safe-area, `h-dvh`, overscroll, manifest palette sync) + Kanban mobile (new `StatusFilterPills` + `useMediaQuery`, refactor `ApplicationKanban` to accept `hiddenColumns`) + touch-target audit + 48-check QA sweep (12 routes × 2 themes × 2 widths) + Stage 4 AC-16 strict debt close. Frontend-only, zero backend/schema/hook-signature changes.
- **Stage 5 entry audit (2026-04-20, main @ `b37bce3`):**
  - PC-1 ✅ `main` HEAD = `b37bce3` at audit start; Stage 4 squash `45b989d` 5 commits down.
  - PC-2 ✅ All Stage 4 files present (`command-palette.tsx`, `use-route-history.ts`, `use-command-palette.tsx`, `theme-aware-toaster.tsx`, `app-shell.tsx`, `manifest.json`, `layout.tsx`).
  - PC-3 ✅ `npm test -- --run` = **350/365** (baseline holds; 15 pre-existing flakes in `telegram-tab`, `job-detail-tracking`, `prompt-editor`).
  - PC-4 ✅ Lint 0 errors / 38 warnings; build green (21 static pages). Known: build shows "Unsupported metadata themeColor" warning — will be fixed by FR-1 (move to `viewport` export).
  - PC-5 ⏳ Dev-server smoke deferred to in-build (per Stage 2a/3/4 precedent).
  - PC-6 ✅ Tasks `KanbanBoard` has `hiddenColumns?: TaskStatus[]` prop (`kanban-board.tsx:24`). **Jobs `ApplicationKanban` has no props interface — stateless** (`application-kanban.tsx:51`). FR-11 rewritten to include the refactor (add prop + filter both PIPELINE_COLUMNS and TERMINAL_STATUSES rows + hide empty sections).
  - PC-7 ✅ Mockup `handoff/mockups/hub-brutalist-v2.html` has `@media (max-width:960px)` section with 4 mobile rules (already captured in HANDOFF.md §8).
  - IC drift — **2 items patched in `b37bce3`**:
    1. **IC-5 (ApplicationKanban stateless)** — FR-11 expanded: add `hiddenColumns?: ApplicationStatus[]` prop, filter both `PIPELINE_COLUMNS` (8) and `TERMINAL_STATUSES` (4), hide empty sections.
    2. **IC-6 (Jobs status export shape)** — clarified: no single `APPLICATION_STATUS_ORDER` export; Jobs uses `PIPELINE_COLUMNS + TERMINAL_STATUSES` (12 total). StatusFilterPills for Jobs receives all 12.
  - IC-7 (manifest palette `#0a0b0e`) and IC-8 (`themeColor` in `metadata`) flagged by audit subagent — **not drift**, they're exactly what FR-5/6/1 plan to fix.
  - IC-11 (no `components/shared/` dir) — trivial, Task 11 creates it.
  - Additional audit notes for plan generation: `TASK_STATUS_ORDER.length = 6`; Jobs = 8 pipeline + 4 terminal = 12; `ReminderList` uses **native `<input type="checkbox">`** (not shadcn `Checkbox`) at 16×16px — wrap in `<label min-h-[44px]>` for hit area; no existing `useMediaQuery` hook — safe to create.
  - Decision: **GO.** Open `feat/redesign-stage-5-mobile-qa` and start from Task 1 (branch open) → Tasks 2-7 (PWA baseline, inline) → Tasks 8-9 (layout verify) → Tasks 10-14 (Kanban mobile, subagent-friendly) → Tasks 15-17 (touch targets) → Tasks 18-21 (QA) → Task 22 (ship).
- **Stage 5 plan shape (22 tasks total, 0 done):** Task 1 branch open (inline). Tasks 2-7 PWA baseline — 6 sequential small commits, inline. Tasks 8-9 responsive grid verify (DevTools, no code unless drift). Tasks 10-12 foundations (useMediaQuery + StatusFilterPills + ApplicationKanban refactor) — TDD, subagent candidate for one bundle. Tasks 13-14 page wiring (Tasks + Jobs), inline for review. Task 15 global touch-target audit via subagent. Task 16 apply fixes inline. Task 17 Command Palette mobile sanity. Tasks 18-21 verify + 48-check QA + Lighthouse + iOS live smoke + AC-16 strict, inline. Task 22 squash merge + push + docs update.
- **Previously shipped (historical — Stage 4 complete on `main`, pushed):** Stage 4 squash `45b989d` 2026-04-20, pushed at `650fc5b`. Frontend-only ⌘K Command Palette + theme-toggle polish with zero backend/schema changes.

---

- **Mode (historical, Stage 4):** Redesign **Stage 4 shipped on `main`** (squash `45b989d` 2026-04-20, pushed to `origin/main` at `650fc5b`). Branch `feat/redesign-stage-4-command-palette-theme-polish` closed (10 per-task commits rolled into one squash). `main` and `origin/main` in sync at `650fc5b` (includes `4df9de5` PRD · `aedf58d` audit-record · `45b989d` Stage 4 squash · `650fc5b` docs-record). Plan at `docs/plans/2026-04-20-redesign-stage-4-command-palette-theme-polish.md` was gitignored throughout. Scope shipped: frontend-only ⌘K Command Palette + theme-toggle polish with **zero backend/schema changes**.
- **Stage 4 outcome summary (squash `45b989d`):**
  - **17 files touched, +1195 / −10 lines.** 7 new files shipped: `hooks/use-route-history.ts` (useSyncExternalStore-backed, 97 lines) + test (6 tests), `hooks/use-command-palette.tsx` (Context-based global ⌘K listener, 57 lines) + test (5 tests), `components/command-palette.tsx` (cmdk-based, 133 lines) + integration test (5 tests), `components/command-palette-trigger.tsx` (header button, 19 lines), `components/theme-aware-toaster.tsx` (sonner wrapper reading `resolvedTheme`, 10 lines). Plus `cmdk@^1.0.0` installed (npm resolved 1.1.1).
  - **Command Palette** (⌘K/Ctrl+K): cmdk `<Command.Dialog>` with brutalist override via `overlayClassName`/`contentClassName`; 620px centered, 94vw on narrow viewports (Q10c). Three sections in order: `QUICK ACTIONS` (2 rows — New task → `/tasks?new=1`, Remind me → `/reminders?new=1`; `New note…` dropped during pre-build audit because `/notes` is a Google Drive viewer with no create UX), `JUMP TO` (10 routes pulled from newly-exported `navSections` in `components/layout/sidebar.tsx` — single source of truth; `Outreach` filtered out for demo role via `useAuth().isDemo`), `RECENT` (last 5 visited routes from `localStorage["hub-recent-routes"]`, section hidden when history is empty, current pathname filtered out of returned list). Client-side fuzzy filter via cmdk's built-in `defaultFilter`. `Command.Empty` renders `"No results"` on zero matches. `loop` prop on `<Command>` gives wrap-around ↑↓ navigation (FR-10c-i). `Esc` closes + clears query (setOpen(false) resets query). `<RadixDialog.Title>`/`<Description>` in `sr-only` span to satisfy Radix a11y requirement (cmdk's `label` prop alone triggers a DialogTitle warning in Radix ≥1.1).
  - **Header trigger** (`[▸ search ⌘K]`): inserted left of `<ThemeToggle />` in `components/layout/header.tsx`. `⌘K` hint hidden on <640px via `hidden sm:inline`.
  - **Dashboard mount**: `<CommandPaletteProvider>` wraps `AppShell` in `app/(dashboard)/layout.tsx`; `<CommandPalette />` is a sibling. The provider runs the global `keydown` listener once — trigger button and palette both consume state via `useCommandPalette()` context.
  - **Theme polish** (FR-12/13, AC-16/17): `app/layout.tsx` `<ThemeProvider>` props changed from `defaultTheme="dark" enableSystem={false}` to `storageKey="hub-theme" defaultTheme="system" enableSystem`. `<Toaster theme="dark" .../>` replaced with `<ThemeAwareToaster />` which reads `useTheme().resolvedTheme` and passes `theme="light"|"dark"` to `sonner`. **No localStorage migration** for the old `theme` key — solo dev, re-pick once.
  - **Quick-action wire-up** (FR-14): `tasks/page.tsx` gained `usePathname` import + new `useEffect` on `searchParams.get("new") === "1"` → `setCreateDialogStatus("new")` → scrub param via `router.replace(pathname + preserved-tags)`. `reminders/page.tsx` gained `useEffect, useRouter, usePathname, useSearchParams` imports + similar effect → calls existing `scrollToQuickAdd()` + focuses `#reminder-title` input (via `document.getElementById` since `<Input id="reminder-title">` already exists in `quick-add-form.tsx:119`) → scrub param. `notes/page.tsx` UNCHANGED (no create flow in UI).
  - **Pre-build audit findings** (patched into PRD `4df9de5` before Task 1, following Stage 2a/3 precedent):
    1. `/notes` has no create UX → `New note…` quick action dropped (QUICK ACTIONS down to 2 rows from mockup's 3).
    2. `/reminders` has no modal → `?new=1` rewritten to scroll + focus inline `<QuickAddForm>` instead of "open dialog".
  - **Drift / surprises during build** (all resolved in-branch):
    1. **React compiler lint rejected `setState` inside `useEffect`** in `use-route-history.ts` — the intuitive `useState + setHistory in effect` pattern is flagged by the `react-hooks/purity` rule. Refactored to `useSyncExternalStore` with a module-level pubsub + `writeStorage` → `notify()` → subscribers re-snapshot from localStorage. Conceptually cleaner (localStorage IS the external store), test contract identical, 6 tests pass unchanged.
    2. **jsdom missing Radix prereqs**: `cmdk`'s `<Command.Dialog>` wraps Radix Dialog, which calls `ResizeObserver` + `Element.prototype.scrollIntoView` at mount. Both missing in jsdom. Added 2 no-op polyfills to `vitest.setup.ts` — non-intrusive, only mounts when `globalThis.ResizeObserver === undefined` / `!Element.prototype.scrollIntoView`.
    3. **Radix a11y enforcement**: cmdk's `label` prop alone triggered a `DialogContent requires a DialogTitle` console warning in Radix ≥1.1. Added `<RadixDialog.Title className="sr-only">` + `<Description>` — `@radix-ui/react-dialog` is already transitively present via cmdk so no new install.
    4. **Context-based hook** (vs. stand-alone): the `useCommandPalette` hook in the original plan was stand-alone (`useState` locally in each consumer). That breaks the trigger-button/palette sharing. Rewired to `createContext` + `<CommandPaletteProvider>` — both consumers read the same state. Global ⌘K listener lives in the provider.
    5. **`useRouteHistory` semantics**: returned list excludes the current pathname (so RECENT doesn't show "this page"), but storage includes all 5 most-recent including current. On reload at `/tasks`, storage `[/tasks, /jobs, /]` → returned `[/jobs, /]`. This asymmetry is intentional and tested (AC-11/12).
  - **Verification at ship:** `npm run lint` 0 errors / 38 pre-existing warnings (none in touched files); `npm run build` green (21 static pages); `npm test -- --run` **350/365** (baseline 334/349 + 16 new — 6 `use-route-history` + 5 `use-command-palette` + 5 `command-palette` integration; 15 pre-existing flakes in `telegram-tab`, `job-detail-tracking`, `prompt-editor` byte-identical). SSR smoke on `/`, `/tasks`, `/tasks?new=1`, `/reminders`, `/reminders?new=1`, `/jobs`, `/login` — all 200, dev log clean. Visual AC walk by owner in browser: ✅ gud across all requested AC (⌘K open, filter, routing, quick actions including scroll+focus reminders, RECENT tracking, Outreach hidden for demo, Esc clears, header button, theme switching, toast chrome sync).
  - **Deferred to post-ship**: AC-16 strict test (system-preference on fresh `localStorage` with macOS light) — requires a truly clean browser profile; recommended during a Stage 5 QA sweep.
  - **Known items (not blockers)**: (a) `notes/page.tsx` remains out-of-palette; if a create-note UX lands later, QUICK ACTIONS can grow back to 3 rows. (b) Global hotkeys `⌘N`/`⌘R`/`⌘⇧N`/`G T` from the mockup are NOT wired (explicit Non-Goal Q4a-i); shortcut-hint column in rows is removed to avoid anti-UX. (c) Entity-level RECENT (tasks/jobs by id) deferred — routes-only is v1. (d) Backend search endpoint not built — palette filter is static over ≤18 rows. All four items are backlog candidates for a future stage.
- **Previously shipped (historical — Stage 3 complete on `main`, pushed):** Stage 3 squash `540c6d4` 2026-04-20 at `origin/main` `deb5735` (includes `86e7c38` PRD · `8dab27e` PRD-patch · `c5f4218` audit-record · `540c6d4` Stage 3 squash · `c3e3c48` docs-record · `deb5735` push-record). Branch `feat/redesign-stage-3-tasks-reminders-jobs` closed (21 per-task commits rolled into one squash). Scope shipped: frontend-only brutalist re-skin of `/tasks`, `/reminders`, `/jobs` with **zero backend/schema/hook/type changes** — existing Kanban status enums (6 on Tasks, 12 on Jobs), all CRUD flows, dnd-kit drag-drop, and mutation wiring preserved identically.
- **Stage 3 outcome summary (squash `540c6d4`):**
  - **26 files touched, +1204 / −411 lines.** Four new helpers/components shipped: `frontend/src/lib/utils/days-since.ts` (+ test), `frontend/src/lib/utils/format-applied-date.ts` (+ test), `frontend/src/components/jobs/jobs-hero.tsx` (+ test), and `frontend/src/components/reminders/__tests__/reminder-list-groups.test.tsx`. Everything else is re-skin in place.
  - **Tasks page** (`/tasks`): Brutalist `.ph` header (kicker + `TASKS_` + `<open> open · <due_today> due today` subline + acid-lime `+ NEW TASK`). `TasksViewToggle` pills + top-right `Analytics` link re-skinned in place — **no new top-level tab bar** (post-audit decision: mockup's `KANBAN · TABLE · ANALYTICS` tab row adds no functional value beyond the existing toggle). Kanban column headers got square markers + uppercase labels + count pills. `TaskCard` border is now 1.5px `var(--line)` with priority left-stripe (3px inline from `PRIORITY_BORDER_CSS_VARS[task.priority]`) preserved as-is — priority-color source left as shadcn-aliased tokens per the Stage 1 alias layer rather than remapped to explicit brutalist tokens (see cosmetic items below). `TasksTable` + `TaskFiltersBar`: pure token swap, behavior identical.
  - **Reminders page** (`/reminders`): Brutalist `.ph` header with `<N> due today · <M> upcoming birthdays` subline (DATA-MAP rule applied — birthdays clause omitted during `biLoading`/`biError` so `0` is never rendered misleadingly). `RemindersTabs` re-skinned above a new 2-column grid (`1.7fr` reminders | `1fr` birthdays aside). `QuickAddForm` at the top of the left column has a dashed-border shell with transparent mono input and a `NATURAL · ⏎` hint. `ReminderList` group headers use an `.hdline` pattern; rows are a `chk | when | body | acts` grid with `r.status === "done"` as the canonical done-predicate (plan's `r.done` was plan-local drift — the actual field is `status: "pending" | "done"`; `completed_at` exists but is informational). `BirthdayList` gained a `limit?: number` prop (1-line addition) — unlimited when absent so `/reminders/birthdays` subroute behavior is unchanged. Quality-of-life additions caught by the Group 2 subagent: native-checkbox `stopPropagation` so checking doesn't trigger row-expand; `Pin` glyph in the `when` cell for floating reminders (plan's `formatTime(r.remind_at)` would have rendered `—` for floating). **No Settings panel** added (FR-R8 explicit OUT) — right column is birthdays-only.
  - **Jobs page** (`/jobs`): Brutalist `.ph` header with `<live> live · <in_interview> in interview · <offer> offer` subline. **NEW component `<JobsHero />`** (~90 lines, extracted to `components/jobs/jobs-hero.tsx` because inline JSX would have crossed the ~80-line threshold) — 4 stat cells (`Applied` / `Screen` / `Interview` / `Offer`) with mini-bars scaled `count / max * 100%` per cell, disjoint bucket semantics (`applied` ≠ `screening` ≠ `interview` ≠ `offer`). `viewMode` default flipped from `"table"` to `"kanban"`. Existing `Jobs` / `Search` / `Analytics` tab labels preserved (uppercased via CSS, NOT renamed to `PIPELINE` — that would have broken the `?tab=search` / `?tab=analytics` URL contract used by cross-page links). Lucide glyphs dropped from tab labels. `ApplicationCard` rewritten: footer reads `{formatAppliedDate(applied_date)} · {daysSince(applied_date)}D` (e.g. `10 APR · 10D`) with `—` fallback for null; `location` + `match_score` compose the optional second footer line (`KanbanCard` has NO salary fields — verified `types/job.ts:92-104` during audit). **Hot variant** (orange border `var(--accent-2)`) triggers when `next_action_date` is in the `-3..0` day window AND status ∈ `{screening, technical_interview, final_interview}`. **Offer variant** (teal border `var(--accent-3)`) on `status === "offer"`. Terminal-status cards go `opacity-60` + muted company name. `ApplicationColumn` headers got square markers + uppercase + count pills; drop-zone uses outline (not ring). `dnd-kit` handlers + `StatusChangeDialog` flow UNTOUCHED — drag-drop regression risk nil. `JobsViewToggle` + `JobsTable`: pure token swap.
  - **Subagent-caught bug during Group 3:** pre-existing `view-toggle.tsx:22` `getStoredView()` called `onChange("table")` on mount when no localStorage preference existed, which would have silently reverted Task 17's `"kanban"` default. Fix landed in `view-toggle.tsx` (now returns `null` when nothing stored, gates `onChange` on having a stored value) and 2 view-toggle tests updated in a separate `test(stage-3): ...` commit (amend forbidden by global rules).
  - **Helpers** (`frontend/src/lib/utils/`): `daysSince(date, now?): number | null` and `formatAppliedDate(date): string` both parse `YYYY-MM-DD` via regex-split (not `new Date(str)`) so the result is timezone-agnostic — otherwise `"2026-04-10"` parses as UTC midnight and shifts by the local offset (verified: on PST the plan's original `new Date(str)` sketch produced `9 APR · 11D` for a `2026-04-10` input on `2026-04-20`). Negative return values from `daysSince` are documented (future dates stay visible rather than being clamped). 10 unit tests.
  - **Verification:** `npm run lint` 0 errors / 38 pre-existing warnings (none in touched files); `npm run build` green (21 static pages); `npm test -- --run` **334 / 349** (baseline 315/330 + 19 new: 6 `days-since` + 4 `format-applied-date` + 2 `reminder-list-groups` + 3 `jobs-hero` + 4 `application-card`; view-toggle tests updated to match the localStorage fix = net flat, not counted toward new). The 15 pre-existing flakes in `telegram-tab`, `job-detail-tracking`, `prompt-editor` are **byte-identical** to Stage 2a close.
  - **Known cosmetic items (all to revisit in post-ship smoke, not blocking ship):**
    1. `PRIORITY_BORDER_CSS_VARS` in `types/task.ts` resolves to shadcn-aliased tokens (`var(--destructive)`, `var(--accent-amber)`, `var(--primary)`, `var(--text-tertiary)`) via the Stage 1 alias layer rather than being directly remapped to brutalist tokens. Task 6 subagent correctly deferred the `types/` edit per hard guard rails. If priority stripes read muddy in smoke, 1-line remap in `types/task.ts:154-159`.
    2. `ApplicationCard` `applied_date = null` → footer shows `—` (no `NOT SET` copy). Kept per brutalist minimalism.
    3. `ApplicationCard` "hot" window is `-3..0` days (imminent action). Overdue `next_action_date` (daysSince > 0) does NOT trigger hot. Semantic is "action soon", not "action overdue" — consistent interpretation.
    4. `ApplicationCardOverlay` (drag state) always renders with default `var(--accent)` border, does not inherit hot/offer variant colors. Drag is brief enough that variant-inheritance isn't load-bearing.
    5. Reminders right-column `BirthdayList` row internals still use shadcn tokens (`rounded-lg + bg-card`) — visual seam against the brutalist reminders column. Not in Group 2 Task scope; a future 1-commit polish can re-skin the birthday row shell.
    6. Reminders-adjacent internals not touched in Stage 3: `EditReminderForm`, `EditReminderDialog`, snooze popover, `CompletedRemindersSheet`. Still shadcn-styled; flag for smoke audit if the visual seam is noticeable.
  - **PC-6 (dev-server live smoke) deferred to post-ship** per Stage 2a precedent. Recommended AC pass when a live smoke happens: AC-1…AC-18 from the PRD, especially AC-5 (Tasks Kanban drag-drop, no StatusChangeDialog), AC-10 (JobsHero counts match manual filter), AC-11 (mini-bar scaling + Interview orange / Offer teal), AC-12 (zero-case renders `0` not `—`), AC-13 (Jobs Kanban default + drag opens StatusChangeDialog), AC-14 (DAYS SINCE null case renders `—`), AC-15 (hot + offer variants visible).
- **Previously shipped (historical — Stage 2a + post-ship smoke complete on `main`):** Stage 2a squash `e481587` 2026-04-19; 3 follow-up fixes on 2026-04-20 `0274652`/`49d6a7f`/`3c657b2`. Planner UI overlay on Today: `PlanBar + FocusQueue + FixedSchedule` when `GET /api/planner/plans/today` returns 200; handoff-Today fallback with `NoPlanStrip` when 404. StatsGrid `Tasks done · today` cell replaced with `Plan adherence · 7d` in both modes (FR-19). Frontend-only — zero backend/schema/skill changes. Stage 2 (squash `a74c16f`) + Stage 1 (squash `9be267f`) shipped earlier same week; Stage 2a audit + doc-patch (commits `a9135fb` + `d16a476`) completed before branch opened.
- **Stage 3 entry audit (2026-04-20, main @ `8dab27e`):**
  - PC-1 ✅ `main` HEAD = `86e7c38` (Stage 3 PRD) at audit start; 4 Stage 2a commits present below.
  - PC-2 ✅ 12 components + `today-date.ts` + `__tests__/` live under `frontend/src/components/today/`.
  - PC-3 ✅ `npm test -- --run` = **315/330** (first attempt 312/330 was a flake; second run reproduced the canonical 315). 15 pre-existing failures in `telegram-tab`, `job-detail-tracking`, `prompt-editor` — byte-identical to Stage 2a close.
  - PC-4 ✅ `backend/app/models/job.py` L68 `status`, L74 `applied_date`, L76 `next_action_date` all present.
  - PC-5 ✅ Mockup sections bookmarked (`handoff/mockups/hub-brutalist-v2.html` ~L564-658 Tasks, ~L660-736 Jobs, ~L738-836 Reminders).
  - PC-6 ⏳ Dev-server live smoke **deferred to post-ship** per Stage 2a precedent.
  - PC-7 ✅ Lint 0 errors / 38 pre-existing warnings; build green (21 static pages).
  - IC drift — **3 items fixed in doc-patch `8dab27e`**:
    1. Tasks has NO existing tab bar (only `TasksViewToggle` pills + top-right Analytics `<Link>`). Mockup's `KANBAN · TABLE · ANALYTICS` tab row would add a new surface with zero functional value. FR-T2 / IC-T8 / FR-T7 / FR-P2 rewritten: re-skin pills + Analytics link **in place**, no new tab bar.
    2. `KanbanCard` (`types/job.ts:92-104`) has **no salary fields** (lightweight view type — only `id, status, title, company, location?, match_score?, applied_date?, next_action?, next_action_date?, created_at, updated_at`). FR-J5 footer reworded to use existing `location` + `match_score` only; Non-Goal + Risk #7 clarified. No new fetches.
    3. Jobs tab labels kept as `Jobs` / `Search` / `Analytics` (not renamed to `PIPELINE` / `SEARCH` / `ANALYTICS`) — preserves URL query-params (`?tab=search`, `?tab=analytics`) used by cross-page links.
  - Plan-only drift (fixed in the local plan, no PRD change needed): `r.done` → `r.status === "done"` in the ReminderRow code sketch (canonical field is `status: "pending" | "done"`; `Reminder.completed_at` exists but is informational, not the predicate). 4 replacements.
  - Decision: **GO.** Open `feat/redesign-stage-3-tasks-reminders-jobs` and start Task 2.
- **Stage 3 plan shape (26 tasks total, 1 done):** Task 1 audit ✅ shipped as doc-patch on `main`. Tasks 2-8 (helpers + Tasks page) → commit group 1. Tasks 9-14 (Reminders) → commit group 2. Tasks 15-23 (Jobs + `JobsHero`) → commit group 3. Tasks 24-26 (verify/smoke/ship) → commit group 4. Execution plan: Task 2 inline in main session (small, ~60 lines code+tests), commit groups 1-3 delegated to subagents one at a time, group 4 inline for review + squash-merge.
- **Previously shipped (historical — Stage 2a + post-ship smoke complete on `main`):** Stage 2a squash `e481587` 2026-04-19; 3 follow-up fixes on 2026-04-20 `0274652`/`49d6a7f`/`3c657b2`. Planner UI overlay on Today: `PlanBar + FocusQueue + FixedSchedule` when `GET /api/planner/plans/today` returns 200; handoff-Today fallback with `NoPlanStrip` when 404. StatsGrid `Tasks done · today` cell replaced with `Plan adherence · 7d` in both modes (FR-19). Frontend-only — zero backend/schema/skill changes. Stage 2 (squash `a74c16f`) + Stage 1 (squash `9be267f`) shipped earlier same week; Stage 2a audit + doc-patch (commits `a9135fb` + `d16a476`) completed before branch opened.
- **Post-ship smoke outcome (2026-04-20, demo user on local `postgresql://.../personal_hub`, seeded plan_id=1 with 4 items + one Google Calendar event):**
  - AC-1 plan-mode render ✅ — `PLAN · 2026-04-20`, progress 50% (2/4, 30/165M seeded), `▶ NEXT · Fix authentication token refresh bug · 90m`, FocusQueue items with correct chips (`90M` / `CAREER` / `#629` linked-task chip in teal on row 1), done/skipped rows with strike + 0.6 opacity.
  - AC-2 no-plan fallback ✅ — `NoPlanStrip` dashed CTA + Stage 2 Hero/Timeline/StatsGrid (Plan adherence `—`) + SignalsFeed all render.
  - AC-3 optimistic complete + server refresh ✅ — PATCH 200, local state flips instantly, PlanBar recomputes to `3/4 · 75/180M` within ~300ms (server-side `planned_minutes` recalc: 165→180 since server recomputes from sum of items).
  - AC-4 revert on failure + toast ✅ — verified implicitly via the `restrict_demo` 403 gate on PATCH: optimistic flip → 403 → revert → `toast.error("Не удалось отметить — попробуй ещё раз")`. (This flow drove fix #2 below.)
  - AC-5 done-row inert ✅ — click on checked `Buy groceries` → no PATCH in Network, no UI change, no toast.
  - AC-6 FixedSchedule positive ✅ — `Weekly Team Standup 15:21 · 30M` renders sorted (only one event in the seed, sort-correctness not stressed).
  - AC-7 reminder de-dup — **skipped**, seed had no due reminder for 2026-04-20.
  - AC-8 Plan adherence cell ✅ — initially `—` (seeded `adherence_pct=null`), after PATCH → `42%` in the same tab (75/180 = 0.4167, drove fix #3 below).
  - AC-9/10/11/13 — **skipped** (Telegram cross-tab needs bot live; theme/keyboard/Lighthouse are visual checks deferred to Stage 4 era).
- **Smoke-driven fixes shipped during this session (all single-commit squash merges to `main`):**
  - `0274652` — **`fix(today): null-safe PulseDigest.content in SignalsFeed`**. Stage 2 bug exposed by smoke: backend `PulseDigest.content: Optional[str]` but frontend type said `string`, and `signals-feed.tsx:34` did `digest.content.slice(0, 220)`. Demo user's digests have `content=null`; the entire Today page crashed with `TypeError: Cannot read properties of null (reading 'slice')`. Fix: type now `string | null`, render path does `digest.content?.slice(0, 220) ?? ""`. Four usages of `digest.content` grep'd across frontend — only this one was unsafe.
  - `49d6a7f` — **`fix(stage-2a): toast error on PATCH failure in plan complete mutation`**. FR-10 specified `toast.error("Не удалось отметить — попробуй ещё раз")` on PATCH failure; the shipped `useCompleteItemMutation.onError` only reverted the cache. Caught when the 403 from `restrict_demo` reverted silently during smoke. Fix: import `toast` from `sonner` (already project-wide for `use-telegram-bridge`, `use-pulse-settings`, etc.), fire it inside `onError` after the cache rollback. +1 unit test (`mutateAsync` rejected → `toast.error` called with exact Russian string).
  - `3c657b2` — **`fix(stage-2a): invalidate analytics on plan item complete`**. Stage 2a tied analytics refetch to `visibilitychange` (FR-21) only, leaving `Plan adherence · 7d` cell stale after a same-tab PATCH until the user tab-switched. UX gap, not a spec violation — FR-8's "counts update within ~500ms" referred to PlanBar counts (plan query) but implied aggregate consistency. Fix: `useCompleteItemMutation.onSuccess` now invalidates `["planner","analytics","7d"]` and `["planner","analytics","7d-prior"]` alongside the plan key. Visibility refetch remains as safety net for cross-surface completions (Telegram bot). +1 unit test asserting all three keys are invalidated.
- **Smoke-only DB ops reverted:** demo user `role='member' → 'demo'`, seeded `daily_plans.id=1` deleted. No residual state beyond the three commits and the fixture plan's side-effects on `plan_items` (cascade-deleted with the plan).
- **Verification at post-smoke close:** `npm run lint` 0 errors / 38 pre-existing warnings; `npm run build` green; `npm test -- --run` 315/330 (baseline 289/304 at Stage 2 close → Stage 2a +24 → post-smoke fixes +2 = 26 new tests all green; 15 pre-existing flakes in `telegram-tab`, `job-detail-tracking`, `prompt-editor` unchanged). `main` at `3c657b2` pushed to `origin/main`.
- **Stage 2a outcome summary:**
  - Seven new components under `frontend/src/components/today/`: `plan-bar.tsx`, `focus-queue.tsx`, `fixed-schedule.tsx`, `no-plan-strip.tsx`, `plan-adherence-cell.tsx`, `today-skeleton.tsx`; one modified — `stats-grid.tsx` now accepts an optional `replaceTasksDoneWith?: ReactNode` prop (default `undefined` keeps Stage 2 callers identical). Tile skeleton (`p-[14px] flex flex-col gap-1 min-h-[80px] justify-between` + border classes) lives on the StatsGrid side; `PlanAdherenceCell` emits only label + value + optional delta via a React Fragment.
  - Data layer: `frontend/src/types/plan.ts` (DailyPlan / PlanItem / PlannerContext / AnalyticsResponse / PatchPlanItemBody + `labelForCategory` helper + `UNCHECKED_STATUSES` const). `frontend/src/lib/api.ts` extended with `plannerApi.{getPlansToday, patchTodayItem, getContext, getAnalytics}` using `api.get`/`api.patch` (plan's narrative used the name `apiClient`; actual export is `api` — trivial rename at implementation time, behavior unchanged). Four new hooks: `use-plan-today.ts` (query + `useCompleteItemMutation` with optimistic PATCH + `onSuccess` invalidate for server-computed aggregates), `use-plan-analytics.ts` (parallel 7d + prior-7d windows, client-computed `deltaPct`), `use-planner-context.ts` (calendar events for FixedSchedule), `use-visibility-refetch.ts` (tab-focus → invalidate listed query keys).
  - `frontend/src/app/(dashboard)/page.tsx` rewritten as conditional render on `hasPlan`. Plan-mode: `PlanBar` → two-col (`FocusQueue + FixedSchedule` | `HeroCells + StatsGrid + RemindersToday`) → Signals feed. No-plan mode preserves Stage 2 layout exactly with a prepended `<NoPlanStrip />`. The inline `Hdline` helper is kept for no-plan section separators; plan-mode drops `Timeline · Today` / `Reminders · Today` headings (`PlanBar`, `FOCUS QUEUE`, `FIXED SCHEDULE` are self-labeling). `PlanAdherenceCell` renders in both modes per FR-19.
  - **Drift between plan and reality, fixed at write time:**
    - Hook test files: plan specified `.ts` extension for `use-plan-today.test.ts` / `use-plan-analytics.test.ts` / `use-planner-context.test.ts` / `use-visibility-refetch.test.ts`, but each contains JSX (`QueryClientProvider` wrapper). Wrote as `.tsx` — JSX requires the TSX extension to compile.
    - `vi.mock` + spy hoisting: plan's `focus-queue.test.tsx`, `fixed-schedule.test.tsx`, `plan-adherence-cell.test.tsx`, `page-plan-mode.test.tsx` declared the mock-state `const` AFTER the `vi.mock` factory. Vitest hoists `vi.mock` above everything, so the factory's reference would hit TDZ at module-resolution time. Switched all four to `vi.hoisted(() => ({ ... }))` — standard fix, semantics identical.
    - PlanAdherenceCell wrapper: plan's Task 12 code had its own outer `border + padding + flex` `<div>`, then Task 12a said "drop it and let StatsGrid provide the tile skeleton." Wrote it directly as a Fragment from the start (no double-commit churn). Tests assert on text content only, so the structural change didn't affect the tests.
    - Page component name: plan calls it `DashboardPage`; Stage 2 shipped it as `TodayPage` in `app/(dashboard)/page.tsx`. Kept `TodayPage` to avoid a gratuitous rename.
    - `git branch -d` vs `-D`: squash merge creates a new commit SHA, so git refuses `-d` ("not fully merged"). Used `-D` — content is preserved in the squash commit (`e481587`).
  - **Verification at ship:** `npm run lint` 0 errors / 38 pre-existing warnings (none in touched files); `npm run build` green (21 static pages, compiled in ~5s); `npm test -- --run` 313/328 (baseline 289/304 + 24 new tests, all green — 15 pre-existing flakes in `__tests__/telegram-tab.test.tsx`, `__tests__/job-detail-tracking.test.tsx`, `src/__tests__/pulse/prompt-editor.test.tsx` unchanged, no regressions). **Manual smoke test deferred to post-ship per owner decision** — the 12 AC scenarios (AC-1 plan mode, AC-2 no-plan fallback, AC-3 optimistic complete, AC-4 PATCH failure revert, AC-5 done-row inert, AC-6 empty FIXED hides, AC-7 reminder de-dup, AC-8 adherence cell, AC-9 cross-tab refresh, AC-11 keyboard, AC-13 a11y) need live backend + seeded plan data + Telegram bot, not automatable in CI.
  - Branch `feature/redesign-stage-2a-planner` deleted after squash (force delete — squash creates divergent SHA). Not pushed to remote yet. Local `main` is ahead of `origin/main` by one commit.
- **Stage 2a artifacts (all recorded before ship):**
  - PRD (in git): `docs/prd-redesign-stage-2a-planner-ui.md`, updated post-audit in commit `d16a476`. Source of truth.
  - Plan (gitignored, local-only): `docs/plans/2026-04-19-redesign-stage-2a-planner-ui.md` — 17 tasks, 14 per-task commits on the feature branch before squash.
  - Discovery (in git): `docs/discovery-2026-04-19-redesign-and-planner-ui.md`.
  - Audit record: locked into `CLAUDE.md` commit `a9135fb` (PC-1…PC-5 all ✅, GO decision).
- **Stage 2 outcome summary:**
  - Six new components under `frontend/src/components/today/`: `hero-priority.tsx`, `hero-cells.tsx`, `day-timeline.tsx`, `stats-grid.tsx`, `reminders-today.tsx`, `signals-feed.tsx`, plus the `today-date.ts` helper (`todayBounds`, `isSameLocalDay`, `formatTime`, `daysAgo`, `todayStart`). `src/app/(dashboard)/page.tsx` rewritten: Hero row (Priority + Cells) → `Timeline | Stats+Reminders` two-col → Signals feed. The previous Dashboard components (`DashboardClient`, `PulseDigestWidget`, `VitalsWidget`, `RecentActivity`) and the inline "New Task" dialog were removed from Today — those widgets still exist in `src/components/dashboard/` but are no longer mounted on the home route. `globals.css` picks up `@keyframes pulse` for the HeroPriority kicker dot.
  - **Scope constraint preserved** (`handoff/HANDOFF.md` §2): frontend-only. Zero backend, schema, or hook additions. All six components read through existing hooks (`useTasks`, `useJobs`, `useCalendarEvents`, `useReminders`, `useNotes`, `useDashboardSummary`, `useLatestDigest`). React Query keys collapse across components (shared `["tasks", {}]`, `["jobs", {}]`, `["calendar-events", {start, end}]`) — only one fetch per source even though several components subscribe.
  - **Priority_01 algorithm** (in `hero-priority.tsx` `selectPriority`): urgent-today earliest-deadline → any task today earliest-deadline → next upcoming meeting today → empty fallback ("Today is quiet — nothing urgent."). `urgent` in our schema = `P1` in handoff parlance (`TaskPriority` enum is `urgent|high|medium|low`, not `P1/P2/P3`). Countdown ticks every 1s via `useState + setInterval`.
  - **Scope adjustments vs `handoff/HANDOFF.md` §4 / `DATA-MAP.md`** — each driven by a backend gap that `DATA-MAP.md`'s own "не показывать если нет данных" rule applies to:
    - *Focus session* — no `focus_sessions` model in backend. Timeline omits the `NOW` focus block entirely; StatsGrid `Focus · today` cell replaced with `Tasks done · today` (`count(tasks where completed_at = today)`).
    - *Interviews this week* Hero cell — no `job_events` table and no explicit Job↔Calendar link. Replaced with `Tasks due today` (`count(useTasks filtered by deadline == today, status not in done/cancelled)`).
    - *Pulse unread* Hero cell — `PulseDigest` is a category-level digest; no per-item `read=false` flag exists. Replaced with `Meetings today` (`count(useCalendarEvents({start, end}) for today)`).
    - *JUMP TO DRAFT* button in HeroPriority — no `linked_document_id` on `Task`. Button omitted. `OPEN TASK` (or `OPEN MEETING` when priority target is a meeting) and `SNOOZE 1H` remain; snooze PATCHes `deadline += 1h` via `useUpdateTask`.
    - *SignalsFeed custom triggers* (quiet-N-days, stage-move, batch-crossed-to-proposal, etc.) — no aggregation endpoints exist. Each of the four columns simply renders `useLatestDigest(category)` content preview + generation date. Custom-trigger logic deferred beyond Stage 5.
    - *Response rate 30d* (StatsGrid) — no backend aggregator. Computed client-side: `count(jobs where applied_date ≥ today-30d AND status ≠ 'applied') / count(jobs where applied_date ≥ today-30d) × 100`. Rendered with a mini-bar.
    - *Notes 30d + delta* — no date-filter query. Computed client-side from `useNotes()`: count in `[today-30d, today)` minus count in `[today-60d, today-30d)`. OK for a few hundred notes; would need backend work at scale.
    - *Overdue tasks* — preferred source `DashboardSummary.tasks.overdue` (cheap, pre-aggregated by backend). Falls back to 0 if summary unavailable.
  - **Apps live** status set: `applied + screening + technical_interview + final_interview + offer` — matches the "actively in pipeline" reading of handoff's `status IN ('applied','screen','interview')` under our 12-status `ApplicationStatus` enum.
  - **React Compiler lint fix mid-build:** `Date.now()` inside `useMemo` in `day-timeline.tsx` was flagged `react-hooks/purity`. Resolved by lifting `now` into `useState<number>` with a 60s `setInterval` tick — `now` is now a React state, pure within `useMemo`, and the timeline re-evaluates "meeting done" state as time passes. Same pattern was already used in `hero-priority.tsx` (`useCountdown`), but only day-timeline's `Date.now()` appeared in a direct `useMemo` body — hero-priority's was inside a nested helper (`selectPriority`) which the compiler didn't trace into.
  - **Verification at ship:** `npm run lint` 0 errors / 38 pre-existing warnings (none in touched files); `npm run build` green (21 static pages); `npm test -- --run` 289/304 (15 pre-existing failures, unchanged from Stage 1 baseline); dev-server smoke — `GET /login` 200, `GET /` 200 (renders via `AppShell` with no SSR error even with backend down). Visual verification handed to owner (backend was not running at ship-time on the build host; owner confirmed visually before approving merge).
  - Branch `feat/redesign-stage-2-today` deleted after squash. Not pushed.
- **Known out-of-scope items that Stage 2a could touch** (separate from Stage 2a's own scope):
  - Focus session backend model + UI wiring — would un-hide Timeline NOW-block and restore the `Focus · today` StatsGrid cell.
  - Job ↔ Calendar linking (explicit `job_event` or calendar event attribution) — would enable a proper "Interviews this week" metric.
  - `linked_document_id` on Task — would re-enable JUMP TO DRAFT in HeroPriority.
  - Per-item `read` flag on pulse items — would re-enable a "Pulse unread" Hero cell.
  These are orthogonal to Stage 2a (planner UI) and not required for it.
- **Stage 1 outcome summary:**
  - `globals.css` rewritten per `handoff/TOKENS.md`. Brutalist tokens in `:root` (dark defaults): `--bg #0e0e0c`, `--ink #f1efe6`, `--accent #d9ff3d` (acid lime), `--accent-2 #ff6b35` (orange), `--accent-3 #00e0c6` (teal). `html.light` overrides switch to warm-paper palette. Shadcn-namespaced tokens (`--background`, `--primary`, `--border`, etc.) aliased to the brutalist palette so existing shadcn components keep rendering with the new colors without per-component edits. `--radius: 0` everywhere (brutalist = sharp corners).
  - **Token collision fix:** shadcn's `--accent` meaning (subtle hover surface) conflicted with brutalist `--accent` (acid-lime CTA). Resolved via `@theme inline --color-accent: var(--shadcn-accent)` indirection — shadcn components read through `--shadcn-accent` (= `var(--bg-2)`), while direct brutalist CSS reads `var(--accent)` (= acid lime). Two different CSS variables, no collision.
  - `layout.tsx`: Onest dropped, Space Grotesk loaded as display font via `next/font/google`. JetBrains Mono kept for body. `themeColor` meta updated to `#0e0e0c`. `--font-sans` (body default) now points to JetBrains Mono — body is mono.
  - `sidebar.tsx` rewritten: `HUB_` brand + acid-lime square, nav grouped into DAILY / PROJECTS / SIGNALS / ACCOUNT sections, unicode glyphs (◉ ▦ ◷ ◧ ▤ ◈ ▨ ◐ ◇ ▢) instead of lucide icons, acid-lime `border-left: 3px` for active item, footer `● synced · v0.1.0`. Vitals removed from nav (route `/vitals` + page preserved). ACCOUNT section is a deviation from the mockup (not shown in `hub-brutalist-v2.html`) — added to keep Settings / Profile reachable since the mockup has no replacement UX for them.
  - `header.tsx` rewritten: `HUB / <current-page>` breadcrumb (from `usePathname`), live date/time stamp (`MON 19 APR 26 · W16 · 10:42`, ticked every 30s), `◐ THEME` button, avatar → `/profile`, logout. Global stats (FOCUS / INBOX / STREAK from the mockup top bar) intentionally hidden — `handoff/DATA-MAP.md` forbids rendering `0` or `—` for unimplemented metrics, and none of the underlying data sources exist yet (`focus_sessions`, `daily_activity`, unified inbox).
  - `theme-toggle.tsx` simplified: plain brutalist button reading `◐ THEME`, click flips `next-themes` between `dark` and `light`. No hydration-mismatch dance — icon doesn't vary by theme (avoids the "setState in useEffect" react-compiler lint error). `next-themes` config untouched (`attribute="class"` + `defaultTheme="dark"`), so `html` gets `class="dark"` on dark (CSS `:root` still applies; no `.dark` selector exists in `globals.css` anymore) and `class="light"` on light (triggers `html.light` overrides).
  - **Deferred by scope:**
    - Sidebar badge counters `[N]` — deferred to Stage 2a, where the semantic will be two-mode (`count(plan_items status in (pending, in_progress))` when a plan exists, else `count(timeline_rows !done)` from the task+meeting+reminder union). Defining it now would freeze a pre-planner semantic; waiting aligns it with the planner-mode cutover.
    - Top-bar global stats (FOCUS / INBOX / STREAK) — deferred until the data sources land (no backend work in this initiative's scope).
    - `⌘K` command palette button + command palette itself — deferred to handoff Stage 4.
    - Full theme-toggle persist/system-preference work — basic click-flip works; richer theme handling (including `sonner` Toaster theme sync) deferred to Stage 4.
  - **Verification at ship:** `npm run lint` 0 errors / 38 pre-existing warnings (none in touched files); `npm run build` green (21 static pages); `npm test -- --run` 289/304 (15 pre-existing failures in `__tests__/telegram-tab.test.tsx`, `__tests__/job-detail-tracking.test.tsx`, `src/__tests__/pulse/prompt-editor.test.tsx` — identical on `main` before the branch, not caused by Stage 1); compiled CSS verified to contain `#0e0e0c`, `#d9ff3d`, `#f1efe6`, `html.light`, Space Grotesk + JetBrains Mono declarations, no `Onest` references. Live dev-server smoke on `/login` returned 200 with no runtime errors; authenticated pages not visually re-verified in this session (owner to confirm visually before Stage 2 kickoff).
  - Branch `feat/redesign-stage-1-tokens` deleted after squash. Not pushed to remote.
- **Next initiative (queued):** **Stage 2a — planner UI overlay on Today**. Unblocked now that Stage 2 has shipped. Three artifacts already on `main`:
  - Discovery: `docs/discovery-2026-04-19-redesign-and-planner-ui.md` — 8 decisions (Q1…Q8) that lock scope. Key: plan replaces DayTimeline when it exists; light interactivity (checkbox PATCH only); two-section timeline (FIXED calendar + FOCUS QUEUE items); compact Plan-bar instead of big Hero; one Stats Grid cell = `Plan adherence · 7d`; no new `/planner` page.
  - PRD: `docs/prd-redesign-stage-2a-planner-ui.md` — 23 FR, 13 AC, 5 Pre-Implementation Checklist gates (PC-1…PC-5), 10 Interface Contract rows (IC-1…IC-10), Audit Procedure for drift detection.
  - Plan: `docs/plans/2026-04-19-redesign-stage-2a-planner-ui.md` — 16 tasks, 2 commits (scaffolding/data + components/wiring), TDD-style with exact code, ~3-5 focused days.
- **Entry procedure when Stage 2 ships:** (1) run Pre-Implementation Checklist PC-1…PC-5; (2) 10-min Audit Procedure in PRD (verify IC-1…IC-10 against real Stage 2 code); (3) patch PRD + plan if drift found (rename / signature / layout — rules in PRD); (4) only then open `feature/redesign-stage-2a-planner` and start from Task 1. Skipping the checklist is explicitly forbidden in the PRD's top callout.
- **Scope constraints (locked by discovery):** frontend-only; zero backend, schema, skill changes. Consumes already-shipped endpoints (`GET /plans/today`, `PATCH /plans/today/items/{id}`, `GET /context`, `GET /analytics`). Enum correction recorded: `PlanItemStatus = pending / in_progress / done / skipped / rescheduled` (not `planned`).
- **Stage 2a is now unblocked.** Stage 1 shipped (`9be267f`), Stage 2 shipped (`a74c16f`). Handoff spec is in `handoff/HANDOFF.md`, design tokens in `handoff/TOKENS.md`, data-flow in `handoff/DATA-MAP.md`, visual reference `handoff/mockups/hub-brutalist-v2.html`.

---

- **Last shipped initiative:** Telegram→CC bridge **Phase 5 shipped on `main`** (squash `9ac7912`, 2026-04-19). Project switching live — `/project` inline keyboard, per-chat active project, per-(chat, project) sessions with auto-migration of legacy state. Discovery covers 6 sibling projects today. Tests 131 → 163.
- **Feature (most-recently closed):** `telegram-bridge-phase5` — `telegram_bot/projects.py` (auto-discovery of `~/Documents/my_projects/*/CLAUDE.md`, `PROJECT_DENY` deny-list), `state.py` schema rewrite to `{"chats": {cid: {active_project, sessions: {project: uuid}}}}` with silent migration from the Phase 2-4 `{"sessions": {cid: uuid}}` shape, `/project` command + `CallbackQueryHandler`, rewire of `handle_text` / `handle_voice` / `handle_new` / `handle_status` to route cwd + session via the active project, README "Project switching" section.
- **Three-tier data model (live):** Tier 1 `~/Documents/Notes/Personal/` — never reachable via bot (deny + hook in both profiles). Tier 2 `~/Documents/Notes/Personal-mobile/` — reachable only after `/unlock` + PIN. Tier 3 project folders — always reachable subject to per-project `CLAUDE.md`. Owner will populate Personal-mobile with curated data (VIN, plates, insurance numbers, etc.) before real use.
- **Hook threat model caveat:** the hook is a literal substring matcher (`Notes/Personal/`, `Notes/Personal-mobile/` with identifier boundaries). It covers well-behaved skills issuing literal paths. It does **not** cover deliberate obfuscation (`cat ~/Documents/No*/Per*/car.md`, encoded strings, symlinks). True defense-in-depth would require subprocess sandboxing. Accept this boundary.
- **Branch:** `main` (feature branch `feature/telegram-bridge-phase5` deleted after squash).
- **Phase 5 outcome summary:**
  - New: `telegram_bot/projects.py` — `discover(base_dir, deny)` returns sorted `Project(name, path)` for every immediate subdir with a root-level `CLAUDE.md`, minus deny-list. Discovery runs **once at bot startup** only; adding/removing a project requires `launchctl kickstart -k gui/$UID/com.my-personal-hub.telegram-bot`.
  - `config.py` — new `PROJECT_DENY` env field + derived `project_base_dir` / `default_project` / `project_deny_list` from `CC_WORKDIR`. No new required env vars.
  - `state.py` — new schema `{"chats": {<cid>: {active_project, sessions: {<project>: <uuid>}}}}`. Silent migration from legacy on first read (in-process, atomic `_write`). `get_session(chat_id, project)` + `new_session(chat_id, project)` now require project. Default project keeps the `TG_DEFAULT_UUID` no-persist shortcut; non-default projects persist a fresh `uuid4` on first read so resume stays stable. New: `get_active_project(chat_id, default)`, `set_active_project(chat_id, project)`, `configure(default_project)` (called once from `main()`).
  - `main.py` — `/project` renders `InlineKeyboardMarkup` (one row per project, `✅` marking active, callback_data `proj:<name>`). `CallbackQueryHandler(pattern=r"^proj:")` runs the tap through the same `check_sender` + env whitelist gate, edits the original message in place on success. New helper `_active_project(settings, chat_id) -> (project_name, workdir)` reads a module-level `_projects_ref["known"]` populated by `main()` after discovery. `_run_cc_with_optional_progress`, `handle_new`, `handle_status` all route through it. Stale-project (picked earlier but gone now) logs a warning and falls back to default **without rewriting state** — user sees the fallback in `/status` and re-picks explicitly.
  - Security scope: profiles stay global. `Notes/Personal/**` deny + PreToolUse hook run for every project. No per-project overrides in Phase 5.
  - Live smoke (2026-04-19): 6 projects discovered (`barber-booking`, `market-pulse-dashboard`, `mestnie`, `moving`, `my-personal-hub`, `portfolio-site`) ✅. `/project` → keyboard with active `✅ my-personal-hub` ✅. Tap `moving` → "✅ switched to moving" ✅. Text routed to `moving` workdir ✅. `/status` shows `project: moving` at top ✅. `/new` → "🆕 new session in moving: …" ✅. Switch back to `my-personal-hub` → session preserved (CC referenced prior context) ✅.
  - Tests: bot **163 passed** (131 baseline + 8 projects discovery + 10 state migration/per-project/active-project + 14 main handlers/routing/fallback). Backend / frontend untouched.
  - Commits on branch (squashed into `9ac7912`): `bad2abf` projects + config, `d7686bd` state rewrite, `a02137b` /project command, `2ba0a25` active-project routing, `f5c4138` README.
- **Phase 4 outcome summary:**
  - New: `telegram_bot/launchd/com.my-personal-hub.telegram-bot.plist` + `install.sh` + `uninstall.sh`. Hardcoded absolute paths per owner Mac (single-tenant per key constraints). `install.sh` runs `plutil -lint` → copies plist to `~/Library/LaunchAgents/` → `launchctl bootstrap gui/$UID` with fallback to legacy `load`. `uninstall.sh` runs `bootout` with fallback to `unload`, removes plist, and polls up to 5s for the service to actually disappear (macOS `bootout` is async).
  - Log story: primary `~/Library/Logs/com.my-personal-hub.telegram-bot.log` is still rotated in-process by `RotatingFileHandler(5MB × 5)`. Secondary `~/Library/Logs/com.my-personal-hub.telegram-bot.launchd.log` captures launchd stdout/stderr for pre-logger-init tracebacks only — kept crash-only by the `log_setup.py` TTY-guard (StreamHandler attached only when `sys.stderr.isatty()`).
  - Scope addition during build: `log_setup.py` + `tests/test_log_setup.py` were NOT in the original plan ("no Python changes") — added mid-build after smoke showed `.launchd.log` receiving every INFO line (shadow-copy of primary). Owner approved the 1-line TTY-guard fix. Tests 114 → 118.
  - Crash semantics: `KeepAlive={SuccessfulExit:false}` + `ThrottleInterval=60`. Auto-restart on non-zero exit; stable processes (>60s uptime) respawn in ~1s on SIGKILL, unstable ones throttle to 1/minute. Logs confirmed with `kill -9` smoke (PID 18266 → 20210 in 1s).
  - Dev-loop tradeoff: LaunchAgent and a shell-run `python main.py` can't both poll the token. Operator flow documented in `telegram_bot/README.md` — use `./launchd/uninstall.sh` to stop, or `launchctl bootout …` to unload temporarily without removing the plist.
  - Live smoke (2026-04-19): functional (TG text → `🤔 thinking…` → `✅ done` → CC reply) ✅; KeepAlive (kill -9 → new PID in ~1s) ✅; post-respawn functional (context preserved across respawn) ✅; `.launchd.log` empty during steady-state after TTY-guard ✅; uninstall+reinstall idempotent ✅.
  - Tests: bot **118 passed** (Phase 3 baseline 114 + 4 new in `tests/test_log_setup.py` — file handler always attached, StreamHandler present under TTY, absent under launchd, log level propagation). Backend / frontend untouched.
- **Phase 3 outcome summary:**
  - Bot: new modules `request_queue.py`, `progress.py`, `voice.py`. Extended `cc_runner.py` with `run_cc_streaming` (streaming variant for opt-in progress) + `on_spawn` callback on `run_cc` (for `/cancel` to stash the subprocess handle). `main.py` rewritten so every message goes through the per-chat queue; `handle_text` and `handle_voice` share `_run_cc_with_optional_progress` and `_render_result`. `unlock.py` gains `unlock_expires_at` for `/status`.
  - Queue semantics: `MAX_INFLIGHT = 5` counts active + waiting. Msg 1 runs, msgs 2–5 reply `⏳ in queue (pos 1..4)`, msg 6+ replies `⛔ queue full, try later` and is dropped. Plan's original qsize-only math was corrected during Task 1.
  - Voice: faster-whisper `small` CPU int8, lazy-loaded on first voice message, language auto-detect (ru/en). Transcript **always** echoed back as `🎙 transcribed: …` regardless of length (PRD Q4). Handler order: whitelist → enqueue (queue-full rejection runs BEFORE download to avoid wasted Whisper cycles) → download → transcribe → echo → CC.
  - Progress (opt-in): `TELEGRAM_PROGRESS_ENABLED=false` by default; when flipped on, status message is edited on `tool_use` events throttled at ≥10s. No spinner fallback — on `JSONDecodeError` the parser disables itself for the invocation. Task 0 spike pinned the event shape: `tool_use` is a nested block inside `assistant.message.content[]`, `result.result` is the canonical final text, `--verbose` is mandatory alongside `--output-format stream-json`, `stdin=DEVNULL` suppresses the "no stdin data" warning.
  - `/cancel` semantics: SIGINT the active subprocess; accept half-state per PRD Q5. Gates on `proc.returncode is None` to avoid signalling a reaped PID while the worker is still streaming chunks (fixed mid-build after live smoke).
  - Live smoke (2026-04-19): AC-2 voice ✅ (transcript echo + CC reply), AC-8 queue backpressure ✅, `/cancel` pre-finish ✅, `/cancel` post-finish ✅. Progress-flag flip smoke deferred (owner will enable only after ~48h of clean traffic).
  - Tests: bot 114 passed (was 47 at Phase 2 close). Breakdown: 47 Phase 2 baseline + 7 queue + 2 `on_spawn` + 10 handler (/status, /cancel, /help + queue routing + /cancel-after-finish regression) + 27 progress parser + 6 streaming cc_runner + 4 progress-flag routing + 6 voice unit + 4 voice handler + 1 `_render_result` cancel-ghost regression (post-ship `cbd2ad5`). Backend 822 / 9 pre-existing (unchanged from Phase 2 sign-off). Frontend build green.
  - Build-time fixes that shipped alongside the features: `requests==2.33.1` pinned (faster-whisper 1.1.1 imports it without declaring it); `/cancel` returncode gate (see above).
  - Post-ship fix (`cbd2ad5`, 2026-04-19): `/cancel` no longer produces a ghost `Execution error` reply. Root cause: `claude -p` exits **rc=0 with stdout "Execution error"** when SIGINT'd mid-generation, so `_render_result`'s rc==0 path forwarded that string as a fake model reply on top of handle_cancel's `🛑 stopped…`. Fix: `ChatQueueState.cancelled` flag, set by `handle_cancel` right after SIGINT, checked by `_render_result` → edits status to `🛑 cancelled` and skips stdout; reset in worker finally.
- **Anti-abuse guard rails (still load-bearing after the 2026-04-18 freeze):** no spinner fallback ever; ≥10s hard-coded minimum between status edits; `TELEGRAM_PROGRESS_ENABLED` default `false` so default behaviour matches Phase 2's single-edit pattern; `stdin=DEVNULL` on the streaming subprocess; progress parser auto-disables on first `JSONDecodeError`.
- **Key constraints (still in force):** subscription-based `claude -p` only (no Anthropic API); `Notes/Personal/**` denied in both profiles; unlock state in-memory only (restart → re-unlock); CC CLI requires UUID for `--session-id` and rejects in-use UUIDs — `cc_runner._session_file()` probes `~/.claude/projects/<encoded-workdir>/<uuid>.jsonl` and switches to `--resume`; bot on Mac, Hub on Railway; single-tenant. Phase 2 path-syntax quirk stays in effect: `--settings` only matches `Read/Edit/Write/Glob/Grep(~/…)` or `(//abs/…)`, never single-leading-slash absolute paths.
- **Previous phases (closed):**
  - **security-profile-v2** — squash `79c64f0` (2026-04-19). PreToolUse Bash hook + three-tier personal-data model. Tests 118 → 131.
  - **Phase 4** — squash `94316f7` (2026-04-19). launchd LaunchAgent auto-start, KeepAlive + 60s throttle, Python-rotated primary log + crash-only `.launchd.log`. 118 bot tests at close.
  - **Phase 3** — `feat c4f9805` + post-ship `/cancel` rendering fix `cbd2ad5` (2026-04-19). Voice input (faster-whisper CPU int8), per-chat request queue with backpressure, `/help` / `/status` / `/cancel`, opt-in stream-json progress parsing behind `TELEGRAM_PROGRESS_ENABLED`. 114 bot tests at close.
  - **Phase 2** — `feat 378c854` + path-syntax fix `c5a613f` (2026-04-19). Hub-backed `check-sender` whitelist, PIN-gated `/unlock`, locked/unlocked `settings.json` profiles, per-chat `/new` session reset. Backend 822 / 9 / 47 bot tests at close. All AC scenarios (AC-3/4/6/7/9) passed on live bot + deployed Hub.
  - **Phase 1** — `feat f3bc297` + follow-ups. Scaffold, text handler, deterministic `uuid5("tg-default")` session, env-only whitelist.
- **Deferred (still open):** Metal acceleration for faster-whisper (follow-up issue if CPU int8 proves slow). Progress flag smoke test — owner will run after ≥48h of clean traffic on the bot token (ticking from 2026-04-19 Phase 3 ship).
- **Telegram account context (tangential):** owner's original RU-number TG account was session-cascade-terminated during the 2026-04-18 anti-abuse incident and has not yet been recovered (US location + RU SIM makes SMS/voice verification hard). Development continues on a different TG account; `.env` already holds the matching bot token.
- **Next (queued work):** Redesign **Stage 5** — mobile adaptive + QA sweep, per `handoff/PROMPT.md` § Этап 5. Brief: breakpoint `@media (max-width: 960px)` with rules in `handoff/HANDOFF.md` §8; QA checklist covers all pages at ≥960px and ≤760px, both themes, ⌘K working from any page (Stage 4 AC-1/15), all CRUD operations, no console errors, Lighthouse a11y ≥90. Also deferred from Stage 4: strict AC-16 test (system-preference on truly fresh browser profile — requires manual devtools reset). Other open candidates orthogonal to the redesign: progress-flag flip smoke for Telegram bot (~48h clean traffic since Phase 3 ship window now closed — can run on request), Metal acceleration for Whisper, per-project `settings.json` profiles, hot-reload project discovery.
- **Previous initiatives (closed):** Planner↔Hub Phase 2 (`af9a094` + follow-ups); Telegram→CC bridge Phase 1+2+3+4+5 + security-profile-v2 (above).
